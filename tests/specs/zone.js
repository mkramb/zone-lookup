describe("zone screen", function () {
  var ptor;

  beforeEach(function() {
    ptor = protractor.getInstance();
  });

  it("testing, testing", function() {
    expect(true).toEqual(true);
  });
});
